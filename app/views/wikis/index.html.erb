<h1>Wikis</h1>
<% @wikis.each do |wiki| %>
  <div class="media">
    <div class="media-body">
      <h4 class="media-heading">
        <%= link_to wiki.title, user_wiki_path(current_user, wiki.id) %>
      </h4>
    </div>
  </div>
<% end %>
